import type { ReactNode } from "react";

export const metadata = {
    title: "Stream — Adidaya Studio",
    description: "Operational Command Center — Chat-first input for projects, expenses, tasks, and progress updates",
};

export default function StreamLayout({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
